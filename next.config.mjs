/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'lu4zqq7exwahqzug.public.blob.vercel-storage.com',
        port: '',
      },
    ],
  },
};

export default nextConfig;
